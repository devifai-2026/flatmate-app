/**
 * Flatmate Design System — Main export
 * Usage: import { colors, spacing, radius, shadows, typography } from '../theme';
 */

export { colors } from './colors';
export type { ColorKey } from './colors';

export {
  fontFamily,
  fontSize,
  lineHeight,
  letterSpacing,
  textPresets,
} from './typography';

export {
  spacing,
  radius,
  shadows,
  layout,
} from './spacing';
